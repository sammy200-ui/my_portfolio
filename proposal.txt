
[CENTER]
[HEADING1]Adding Native uv Package Manager Support to Hermeto[/HEADING1]

[ITALIC]GSoC 2026 Proposal[/ITALIC]
[/CENTER]

---

[HEADING2]Contact Information[/HEADING2]

[BOLD]Name:[/BOLD] [PLACEHOLDER]
[BOLD]Email:[/BOLD] [PLACEHOLDER]
[BOLD]GitHub:[/BOLD] [LINK @sammy200-ui](https://github.com/sammy200-ui)
[BOLD]Timezone:[/BOLD] IST (UTC+5:30)
[BOLD]University:[/BOLD] [PLACEHOLDER]
[BOLD]Year of Study:[/BOLD] [PLACEHOLDER]

---
[INSERT PAGE BREAK HERE IN GOOGLE DOCS]
---

[HEADING2]Synopsis[/HEADING2]

[CODE]uv[/CODE] is quickly becoming the go-to package manager for Python because of its speed and strict [CODE]uv.lock[/CODE] format. But right now, Hermeto only supports pip. This leaves a major gap: teams building natively with [CODE]uv[/CODE] cannot use Hermeto to guarantee reproducible, network-isolated builds. This proposal addresses that gap by adding a native [CODE]uv[/CODE] backend ([CODE]x-uv[/CODE]) to Hermeto, handling everything from lockfile parsing to dependency fetching and offline environment configuration.

I found Hermeto when the GSoC 2026 organizations were announced, and I have spent the last three weeks digging in. I set up the dev environment, ran the test suites, and mapped out exactly what this [CODE]uv[/CODE] integration requires. Before this, I spent four months actively contributing to AsyncAPI — mostly handling maintenance, bug fixes, and PR reviews. That experience taught me how to jump into an established codebase, understand the existing patterns, and get to work.

At its core, Konflux is about software supply chain security. Because the [CODE]uv.lock[/CODE] format records each dependency with its source, version, and SHA256 checksum for every wheel and sdist, it is actually a perfect fit for secure, deterministic builds. Adding native support for it is not just about keeping up with a new tool — it directly strengthens Hermeto's main mission.

---

[HEADING2]Understanding the Problem[/HEADING2]

Hermeto has a clear [LINK AI Contribution Policy](https://github.com/hermetoproject/hermeto/blob/main/AI_CONTRIBUTION_POLICY.md), so I want to be completely transparent about my workflow. I used AI to help me quickly navigate the codebase, get up to speed on unfamiliar Python patterns, and map out the broader [CODE]uv[/CODE] ecosystem.

However, I did not just generate answers. I manually verified every technical conclusion, architectural mapping, and implementation step outlined below through my own code inspection and local testing. I fully understand the architecture I am proposing, and I am ready to explain and defend all of it.

[HEADING3]What Hermeto Actually Does[/HEADING3]

Hermeto works in two steps. First, [CODE]fetch-deps[/CODE] reads a project's lockfile, downloads every listed dependency, verifies checksums, and stores everything in a flat output directory ([CODE]deps/<pm>/[/CODE]) alongside a CycloneDX SBOM ([CODE]bom.json[/CODE]). Second, [CODE]generate-env[/CODE] and [CODE]inject-files[/CODE] configure the build environment so the package manager uses the local cache instead of the internet. The actual build then runs with zero network access.

Here is how I see the [CODE]uv[/CODE] backend fitting into this flow:

[CENTER]
[BOLD]INSERT DIAGRAM IN GOOGLE DOCS:[/BOLD]
[ITALIC]To add this diagram:
1. Go to https://mermaid.live
2. Paste the code below
3. Click "Save as PNG" and paste the image into your Google Doc
4. Size the diagram so it is compact — either keep as one diagram 
or split into two smaller ones (request flow + output structure)[/ITALIC]
[/CENTER]

```mermaid
flowchart TD
    A("hermeto fetch-deps '{\"type\": \"x-uv\"}' --source ./my-project") --> B("cli.py (parses command)")
    B --> C("resolver.py (dispatches to x-uv handler)")
    
    C --> D{"uv/ backend (NEW)"}
    
    D -->|"1. Read uv.lock"<br>"2. Extract package list"<br>"3. Resolve download URLs"| E("http_requests.py (downloads + checksum verify)")
    
    E --> F("sbom.py (generates CycloneDX BOM)")
    
    F --> G[("hermeto-output/")]
    G --- H1("deps/uv/ (packages)")
    G --- H2("bom.json (SBOM)")
    G --- H3(".build-config.json")
```

[HEADING3]Why uv Needs Its Own Backend[/HEADING3]

This is something I spent time looking into beyond just the idea description.

Yes, [CODE]uv[/CODE] is backwards-compatible with pip. You can run [CODE]uv pip install -r requirements.txt[/CODE] and it works. The mentors explicitly called this out in the project description — that is NOT the native way. [CODE]uv[/CODE] has its own workflow based on [CODE]uv lock[/CODE] and [CODE]uv sync[/CODE], and it produces its own lockfile: [CODE]uv.lock[/CODE].

The [CODE]uv.lock[/CODE] format is very different from pip's [CODE]requirements.txt[/CODE]. It is a TOML file that records each dependency with its name, version, source registry, and inline sdist/wheel entries — each containing the full download URL and SHA256 hash. It also tracks dependency markers for different platforms. The pip backend's [CODE]requirements.py[/CODE] parser will not work for this — a new parser is needed.

[HEADING3]What I Know About the Technical Challenges[/HEADING3]

Alexey Ovchinnikov said something in [LINK Discussion #1396](https://github.com/orgs/hermetoproject/discussions/1396) that shaped how I am writing this proposal:

[QUOTE]"I think this should not be a design document, but a document conveying your capability to write a comprehensive design document and then implement it."[/QUOTE]

So instead of dumping everything I have learned about [CODE]uv.lock[/CODE] internals, here is what I see as the key challenges and how I would approach them:

[BOLD]1. Parsing uv.lock[/BOLD]
The [CODE]uv.lock[/CODE] file is valid TOML, so Python's [CODE]tomllib[/CODE] can parse it. The challenge is not the format but the schema — understanding how packages, sources, wheels, and markers are structured inside the file. Here is a simplified snippet of what a [CODE]uv.lock[/CODE] entry looks like:

[BOLD]SEE SCREENSHOT FILE: uv_lock_snippet.toml (screenshot this and paste into Google Doc)[/BOLD]

The file starts with a header: [CODE]version = 1[/CODE], [CODE]revision = 3[/CODE], and [CODE]requires-python[/CODE]. Each [CODE][[package]][/CODE] section has name, version, source, dependencies, and inline [CODE]sdist[/CODE] and [CODE]wheels[/CODE] entries — each wheel contains its full PyPI download URL and SHA256 hash. The parser would need to walk through these entries, build a list of what to download, and verify checksums. I also want to follow the patterns of Hermeto's existing parsers ([CODE]requirements.py[/CODE] for pip, [CODE]yarn/[/CODE] backend for [CODE]yarn.lock[/CODE]).

I plan to start with simple single-package projects and work up to more complex cases during the research phase.

[BOLD]2. Download URL Resolution[/BOLD]
From looking at real [CODE]uv.lock[/CODE] files, the lockfile already contains full download URLs for each wheel and sdist — for example, [CODE]https://files.pythonhosted.org/packages/.../requests-2.31.0-py3-none-any.whl[/CODE]. This means the backend does not need to query the PyPI simple API at fetch time, it can download directly from the URLs in the lockfile. The pip backend already handles PyPI downloads through [CODE]http_requests.py[/CODE] — I would reuse that infrastructure rather than writing something from scratch.

The source registry field ([CODE]{ registry = "https://pypi.org/simple" }[/CODE]) tells us where the package came from originally, but the actual download URLs are baked into the [CODE]sdist[/CODE] and [CODE]wheels[/CODE] entries. This simplifies the fetching logic compared to pip where download URLs need to be resolved separately.

[BOLD]3. Security — No Arbitrary Code Execution[/BOLD]
This is a core Hermeto rule. From what I have read, [CODE]uv[/CODE] does not run [CODE]setup.py[/CODE] during resolution — it reads metadata statically. That would align with Hermeto's security model. But I have not fully verified this yet and it is one of the first things I need to confirm properly during the research phase. If there are edge cases (like source distributions that require a build step), those need to be scoped out or handled in the design doc.

This is important enough that I would want to trace through [CODE]uv[/CODE]'s actual source code during the research phase rather than just trusting what the documentation says. The mentors specifically listed "inspecting uv's internals to verify whether there's a potential arbitrary code execution path" as an expectation, so this cannot be hand-waved.

[BOLD]4. SBOM Integration[/BOLD]
Each fetched package needs to become a CycloneDX component in the SBOM. The pip backend already maps Python packages to [CODE]pkg:pypi/[/CODE] PURLs through [CODE]sbom.py[/CODE]. The [CODE]uv[/CODE] backend would produce the same PURL entries — the packages come from the same ecosystem (PyPI), just resolved through a different tool.

[BOLD]SEE SCREENSHOT FILE: purl_output_snippet.txt (screenshot this and paste into Google Doc)[/BOLD]

These follow the same [CODE]pkg:pypi/[/CODE] format Hermeto already generates for pip packages (I verified this in the test suite at [CODE]tests/unit/package_managers/pip/test_main.py[/CODE]). The tricky part is ensuring metadata accuracy for packages with non-PyPI sources — git dependencies and direct URLs need special PURL qualifiers.

Also, since the [CODE]uv[/CODE] backend would launch as experimental ([CODE]x-uv[/CODE]), the SBOM needs to include the experimental backend annotations. From the design template, experimental backends mark their components with annotations like [CODE]hermeto:backend:experimental:x-uv[/CODE]. This needs to be wired up from the start.

[BOLD]5. Build Environment Configuration[/BOLD]
After fetching, Hermeto needs to tell [CODE]uv[/CODE] to use the cached dependencies instead of the network. [CODE]uv[/CODE] has environment variables for cache directory and offline mode that should work for this. I still need to dig into the exact configuration during the research phase — figuring out what env vars and config files are needed so that [CODE]generate-env[/CODE] and [CODE]inject-files[/CODE] produce the right output.

The key question here is whether [CODE]uv sync[/CODE] can be pointed entirely at a local cache with no network fallback. If it can, the [CODE]generate-env[/CODE] step just needs to emit the right env vars. If not, there may be config files that need to be injected. This is one of those things that is easier to figure out by testing locally than by reading docs, so I plan to experiment with this early in the bonding period.

[BOLD]A note on output directory structure:[/BOLD] Hermeto's [CODE]fetch-deps[/CODE] typically downloads wheels and sdists into a flat output directory (e.g. [CODE]deps/pip/[/CODE] for pip, [CODE]deps/npm/[/CODE] for npm). [CODE]uv[/CODE], on the other hand, has a highly specific, complex internal cache structure ([CODE]UV_CACHE_DIR[/CODE]). A key research deliverable is figuring out how to feed Hermeto's flat downloaded files into [CODE]uv sync --offline[/CODE]. I will investigate whether to construct a local index ([CODE]--find-links[/CODE] or a local file URI index) or if [CODE]uv[/CODE] exposes a way to directly consume flat files without its internal cache layout. This is a friction point the design document will need to solve before any implementation starts.
 
[BOLD]6. Edge Cases I Am Watching For[/BOLD]
[BULLET] Git dependencies in [CODE]uv.lock[/CODE] — these are not PyPI packages and need different fetching logic
[BULLET] Platform-specific wheel selection — [CODE]uv.lock[/CODE] records wheels for multiple platforms; Hermeto needs to decide which to fetch
[BULLET] Source distributions without pre-built wheels — these might require a build step, which conflicts with the no-arbitrary-code rule
[BULLET] Workspace/monorepo support — [CODE]uv[/CODE] supports workspaces with multiple packages; the lockfile structure gets more complex
[BULLET] The [CODE]uv.lock[/CODE] format is not formally specified as a stable API yet — Astral (the team behind [CODE]uv[/CODE]) moves exceptionally fast and the format is still evolving. [BOLD]Mitigation:[/BOLD] I will design the parser to explicitly check the [CODE]version[/CODE] field in the lockfile header (currently [CODE]version = 1[/CODE]). If Astral introduces a breaking lockfile change, Hermeto should gracefully fail or alert the user rather than silently producing incorrect results — preventing broken CI/CD pipelines. During Phase B1 (Weeks 3-4), I have buffer time allocated to adapt the parser if upstream format shifts appear during the research phase.

I do not have answers to all of these yet. That is exactly what the research phase and design document are for. [BOLD]But I do think knowing the right questions to ask is half the work, and I would rather be honest about what I do not know than pretend I have it all figured out.[/BOLD]

---

[HEADING2]Implementation Plan & Timeline[/HEADING2]

The expected deliverables from the project description are clear: research, design document, implementation, SBOM integration, tests, and documentation. Here is how I plan to sequence them.

[HEADING3]Phase A: Research and Design Document[/HEADING3]
[ITALIC](Community Bonding + Weeks 1-2)[/ITALIC]

Nothing else can start until this is done. Before writing any backend code, I need to produce a design document using the project's own template at [CODE]docs/design/package-manager-template.md[/CODE]. I went through the template and it covers:

[BULLET] An overview of the package manager and developer workflow
[BULLET] How dependency lists are generated and what format they follow
[BULLET] How checksums work
[BULLET] Whether Hermeto should fetch directly or trust the PM's native fetch
[BULLET] Build environment configuration (env vars, config files)
[BULLET] Implementation notes and current limitations

The design document is not just paperwork — it is the blueprint the mentors will review before I start building. I will submit it as a PR to [CODE]docs/design/[/CODE] and iterate based on Erik and Alexey's feedback.

During Community Bonding, I will also:
[BULLET] Set up local test environments with real [CODE]uv[/CODE] projects of varying complexity
[BULLET] Study the existing pip backend closely — it is my reference implementation
[BULLET] Inspect the [CODE]uv.lock[/CODE] output for different project types to identify edge cases

[HEADING3]Phase B: Core Implementation[/HEADING3]
[ITALIC](Weeks 3-7)[/ITALIC]

Once the design document is approved, I build. The work breaks down into:

[BOLD]B1. Lockfile Parser (Weeks 3-4)[/BOLD]
[BULLET] Create [CODE]hermeto/core/package_managers/uv/[/CODE] module
[BULLET] Write the [CODE]uv.lock[/CODE] parser that extracts dependency names, versions, sources, and checksums
[BULLET] Unit tests for the parser with various lockfile shapes

[BOLD]B2. Fetch Pipeline (Weeks 4-5)[/BOLD]
[BULLET] Implement the main [CODE]fetch_uv_source()[/CODE] function
[BULLET] Download packages using Hermeto's existing [CODE]http_requests.py[/CODE]
[BULLET] Checksum verification against lockfile hashes
[BULLET] Register as [CODE]x-uv[/CODE] in the resolver with the experimental prefix

[BOLD]B3. SBOM Integration (Weeks 5-6)[/BOLD]
[BULLET] Map uv package metadata to CycloneDX components
[BULLET] Generate accurate [CODE]pkg:pypi/[/CODE] PURLs
[BULLET] Experimental backend annotations in SBOM (as documented in the design template for [CODE]x-[/CODE] backends)

[BOLD]B4. Build Environment (Weeks 6-7)[/BOLD]
[BULLET] Implement [CODE]generate-env[/CODE] support — emit the right environment variables for offline [CODE]uv[/CODE] usage
[BULLET] Implement [CODE]inject-files[/CODE] support — write any config files [CODE]uv[/CODE] needs to use the local cache

By the [BOLD]midterm evaluation[/BOLD], the goal is: [CODE]hermeto fetch-deps '{"type": "x-uv"}'[/CODE] works end-to-end on a straightforward [CODE]uv[/CODE] project — it reads the lockfile, downloads packages, verifies checksums, generates an SBOM, and the build environment can use the cached deps offline.

[HEADING3]Phase C: Testing, Docs, and Hardening[/HEADING3]
[ITALIC](Weeks 8-10/12)[/ITALIC]

[BOLD]C1. Integration Tests (Weeks 8-9)[/BOLD]
[BULLET] Create end-to-end tests in [CODE]tests/integration/[/CODE] — containerized tests with real [CODE]uv[/CODE] projects
[BULLET] Alexey confirmed in Discussion #1396 that e2e tests are expected and "rather straightforward" to add in Hermeto
[BULLET] Cover happy path + edge cases identified in the design doc

[BOLD]C2. Documentation (Week 9-10)[/BOLD]
[BULLET] Write user-facing docs page in [CODE]docs/[/CODE] explaining how to use the [CODE]x-uv[/CODE] backend
[BULLET] Include: prerequisites, example usage, known limitations

[BOLD]C3. Hardening and Stretch Goals (Weeks 10-12, if time)[/BOLD]
[BULLET] Handle additional edge cases surfaced during testing
[BULLET] Possible graduation from [CODE]x-uv[/CODE] to [CODE]uv[/CODE] if mentors approve
[BULLET] Stretch goals TBD with mentors based on progress

[HEADING3]Summary[/HEADING3]

[BOLD]Format this as a table in Google Docs with the columns shown below:[/BOLD]

| Phase | Weeks | Milestone | Key Deliverable |
|-------|-------|-----------|------------------|
| A: Research & Design | Bonding + Wk 1-2 | Design doc approved | PR merged in [CODE]docs/design/[/CODE], local test setups ready |
| B1: Parser | Wk 3-4 | [CODE]uv.lock[/CODE] parsing works | Parser extracts packages, versions, checksums with unit tests |
| B2: Fetch Pipeline | Wk 4-5 | Downloads work | [CODE]fetch-deps x-uv[/CODE] downloads and verifies packages |
| B3: SBOM | Wk 5-6 | SBOM generation works | [CODE]bom.json[/CODE] accurately records all fetched [CODE]uv[/CODE] packages |
| B4: Build Env | Wk 6-7 | Offline builds work | [CODE]generate-env[/CODE] + [CODE]inject-files[/CODE] produce correct config |
| [BOLD]Midterm[/BOLD] | Wk 7 | [BOLD]End-to-end demo[/BOLD] | [BOLD]Full [CODE]x-uv[/CODE] pipeline works on standard projects[/BOLD] |
| C1: Integration Tests | Wk 8-9 | E2E tests pass | Containerized tests in [CODE]tests/integration/[/CODE] covering happy path + edge cases |
| C2: Docs | Wk 9-10 | User docs published | Docs page with usage, prerequisites, known limitations |
| C3: Polish | Wk 10-12 | Final submission | Edge case hardening, possible [CODE]x-uv[/CODE] → [CODE]uv[/CODE] graduation |

[BOLD]A note on these targets:[/BOLD] These are goals, not unbreakable promises. If the design review or upstream [CODE]uv[/CODE] changes require adjusting scope, I will communicate that early with the mentors. What I can promise is 25-30 hours a week, consistently, for the full duration.

---
[INSERT PAGE BREAK HERE IN GOOGLE DOCS]
---

---
[INSERT PAGE BREAK HERE IN GOOGLE DOCS]
---

[HEADING2]Previous Contributions[/HEADING2]

The Hermeto GSoC guide says one merged PR is the bar. I have that, but I also want to show the four months of open source work I did before Hermeto.

[HEADING3]Hermeto[/HEADING3]

[BOLD]Format this as a table in Google Docs with the columns shown below:[/BOLD]

| PR / Issue | What I Did | Status |
|------------|-----------|--------|
| [LINK PR #1354](https://github.com/hermetoproject/hermeto/pull/1354) | Replaced fragile automatic test-exclusion logic with standard [CODE]pytest -k[/CODE] filtering, closing mentor-opened issue [LINK #950](https://github.com/hermetoproject/hermeto/issues/950). Reviewed by a-ovchinnikov, merged by eskultety. | [BOLD]Merged[/BOLD] ✅ |

It was a small change but it required actually understanding the test infrastructure — why the exclusion logic was there, why it was causing problems, and why removing it was safe.

[HEADING3]AsyncAPI (4 months before switching orgs)[/HEADING3]

I was contributing to AsyncAPI for about four months before they got rejected from GSoC 2026. Mostly maintenance stuff.

[BOLD]Format this as a table in Google Docs with the columns shown below:[/BOLD]

| PR / Issue | What I did | Category | Status |
|------------|-----------|----------|--------|
| [LINK #4781](https://github.com/asyncapi/website/pull/4781) | Removed deprecated scripts from the repo | Refactoring | Merged |
| [LINK #4997](https://github.com/asyncapi/website/pull/4997) | Cleaned up unused dependencies from package.json | Refactoring | Merged |
| [LINK #4804](https://github.com/asyncapi/website/pull/4804) | Fixed react-hooks/exhaustive-deps warnings across the codebase | Refactoring | Merged |
| [LINK #5014](https://github.com/asyncapi/website/pull/5014) | Replaced Moment.js with Day.js, cutting the bundle size by ~90% | Infrastructure | Merged |
| [LINK #4710](https://github.com/asyncapi/website/pull/4710) | Fixed background scrolling bug on the Roadmap modal | Bug Fix | Merged |
| [LINK #4585](https://github.com/asyncapi/website/pull/4585) | Fixed broken links in git-workflow.md | Bug Fix | Merged |
| [LINK #4600](https://github.com/asyncapi/website/pull/4600) | Fixed broken community links in contributor docs | Bug Fix | Merged |
| [LINK #4988](https://github.com/asyncapi/website/pull/4988) | Reviewed and tested the Navbar Chevron UI fix | Review | Review |
| [LINK #4808](https://github.com/asyncapi/website/pull/4808) | Verified the Algolia search language sync logic | Review | Review |
| [LINK #4893](https://github.com/asyncapi/website/pull/4893) | Helped a contributor understand the codebase logic during their PR | Review | Review |

None of this was glamorous and that was kind of the point.

---
[INSERT PAGE BREAK HERE IN GOOGLE DOCS]
---

[HEADING2]Benefits to the Community[/HEADING2]

Right now, any Python team using [CODE]uv[/CODE] with Konflux cannot do hermetic builds because Hermeto only speaks pip. [CODE]uv[/CODE] is not niche anymore — a lot of projects are moving to it. This backend closes that gap.

There is also a secondary benefit. The pip backend is older and more complex. A clean [CODE]uv[/CODE] backend gives future contributors a better reference if they ever need to add support for Poetry, PDM, or whatever comes next. And the design document itself is useful even if the code takes longer than expected — right now there is no written-down analysis of how [CODE]uv[/CODE] should integrate with Hermeto.

---

[HEADING2]About Me[/HEADING2]

I am a [PLACEHOLDER: year] student at [PLACEHOLDER: university]. Most of my experience is in JavaScript and TypeScript — React, Next.js, Node.js. Python is more recent but I have been spending a lot of time in it lately, and Hermeto's codebase is a good fit for where I am. The patterns are clear and I can follow them.

I am also part of my college's Student Developer Club where I help organize meets, hackathons, and work on projects with others. A lot of it is coordination — less about code and more about making sure things actually get done.

A few projects I built outside of open source:

[BULLET] [BOLD]SiliconSage[/BOLD] — AI-powered PC building assistant. Next.js and TypeScript on the frontend, FastAPI and Scikit-Learn on the backend for ML predictions. [LINK GitHub](https://github.com/sammy200-ui/SiliconSage)

[BULLET] [BOLD]Let's Collab[/BOLD] — Real-time collaborative whiteboard built with a team of four. React, Socket.IO, Node.js, MySQL, Redis. I was not just writing code on this one — I was reviewing what teammates pushed and making sure things actually worked together. [LINK GitHub](https://github.com/IronwallxR5/Let-s_Collab)

[BULLET] [BOLD]NotesApp[/BOLD] — Full-stack note-taking app with AI features: summarize, expand, auto-tag. React, Express, MongoDB, Groq SDK. [LINK GitHub](https://github.com/sammy200-ui/NotesApp)

GitHub: [LINK @sammy200-ui](https://github.com/sammy200-ui)

---
[INSERT PAGE BREAK HERE IN GOOGLE DOCS]
---

[HEADING2]Availability & Commitments[/HEADING2]

My college has an internship period from June to December. If I get selected for GSoC, that becomes my internship — no classes, no exams, no conflicts for the entire coding period.

I can put in 25-30 hours a week. Timezone is IST (UTC+5:30), I usually work during the day but I am flexible. Happy to do async (GitHub, email) or sync calls with Erik and Alexey, whatever works better for them.

---

[HEADING2]Post GSoC[/HEADING2]

I want to stay on after GSoC ends. Not as a vague commitment to sound good in a proposal, but because the work itself makes sense to continue.

The [CODE]x-uv[/CODE] backend, if it ships successfully, will need maintenance. Astral moves fast and [CODE]uv.lock[/CODE] will keep evolving. Someone who already understands the parser and knows the edge cases is better placed to handle that than someone starting fresh. I would rather be that person than hand it off and hope.

There is also the graduation path. The plan is to ship as [CODE]x-uv[/CODE] first — experimental, no stability guarantees. If the implementation holds up through the GSoC period and passes the mentors' review, graduating it to a stable [CODE]uv[/CODE] backend is the natural next step. I want to be around for that.

More broadly, Hermeto is the kind of project I would keep contributing to regardless. It is Python, the codebase is well-organized, the mentors are responsive, and the problem it solves — reproducible hermetic builds — is genuinely important infrastructure work. That is not a combination you find often.
